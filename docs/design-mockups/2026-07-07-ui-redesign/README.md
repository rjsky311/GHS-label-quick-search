# UI Visual Redesign — Locked Reference (2026-07-07)

Locked direction: **Graphite Editorial (Paper × Bench)**.

- `R2-warm-strong-paper.html` — the single self-contained reference mockup.
  Open in a browser; the top-right button toggles the two themes:
  - **light** = Comfort Dim, warm paper with strong grain
  - **dark** = Dark Bench, warm-neutral graphite
  Both use the E editorial layout, Fraunces serif + IBM Plex, and a
  graphite + teal color system. The white label preview stays white in
  both modes (print truth).
- `GHS05.svg` / `GHS07.svg` — real pictograms the mockup references.

Implementation handoff / token extraction / phased plan:
`docs/superpowers/plans/2026-07-07-ui-visual-redesign-graphite-editorial.md`

The multi-round exploration mockups (A–R) were removed after the direction
was locked; the decision journey is summarized in the handoff plan.
